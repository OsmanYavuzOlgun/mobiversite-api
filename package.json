{
  "name": "mobiversite-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "vercel-build": "echo 'Nothing to build'"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
